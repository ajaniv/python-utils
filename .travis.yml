# python-utils travis config file
# TODO: change email address

language: python

matrix:
    include:
        - python: 2.7
          env: TOXENV=py27
        - python: 3.5
          env: TOXENV=py35
       
install:
  - travis_retry pip install tox

script:
  - travis_retry tox

notifications:
  email:
    recipients:
      - amnon.janiv@ondalear.com

    on_success: change
    on_failure: always
  
branches:
  only:
    - master
    - develop